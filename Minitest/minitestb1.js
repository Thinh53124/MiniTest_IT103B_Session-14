let number = prompt("Nhập một số nguyên: ");

for (let i = 0; i <= number; i++) {
    if (i%2 == 0) {
        document.writeln("Số " + i + " là số chẵn" + "<br>");
    }else {
        document.writeln("Số " + i + " là số lẻ" + "<br>");
    }
}

while (n > 1) {
    if (n = "-" + number) {
        console.log("Đây là số âm");
    } else if (n = number) {
        console.log("Đây là số dương");
    } else {
        console.log("Giá trị n không hợp lệ để tạo dãy số");   
    }
}